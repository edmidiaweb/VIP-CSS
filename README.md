# VIP-CSS
VIP CSS: Visualizador Interativo de Propriedades CSS

O VIP CSS (Visualizador Interativo de Propriedades CSS) é uma ferramenta simples, porém poderosa, construída com HTML, CSS e JavaScript puro. Seu objetivo é permitir que desenvolvedores e designers manipulem visualmente as propriedades CSS mais complexas em tempo real, gerando o código exato necessário para uso em seus projetos.A caixa de teste é fixa na tela, garantindo que você visualize as alterações dos controles mesmo ao rolar a página.✨ Funcionalidades PrincipaisO projeto oferece controle interativo sobre as seguintes propriedades CSS:PropriedadeTipo de ControleAçãoBox-ShadowSliders e Seleção de CorAjusta deslocamento X/Y, desfoque, espalhamento, cor e inset.Border-Radius4 Sliders IndependentesControla a curvatura de cada um dos quatro cantos da caixa.TransformSlidersAplica Rotação (rotate) e Escala (scale) em tempo real.FilterSlidersAdiciona efeitos visuais como Desfoque (blur) e Brilho (brightness).OpacitySliderControla a opacidade (transparência) do elemento.Text-ShadowSliders e Seleção de CorAplica sombra ao texto dentro da caixa, controlando deslocamento X/Y e cor.TransitionSlider e DropdownDefine a Duração e a Função de Temporização (easing) para animar as mudanças de estilo.💻 Como UtilizarO VIP CSS não requer dependências ou instalação de pacotes.Clone ou Baixe: Baixe ou clone os três arquivos principais:index.html (Estrutura)style.css (Estilização)script.js (Lógica Interativa)Abra: Abra o arquivo index.html diretamente no seu navegador.Interaja: Comece a arrastar os sliders e selecione cores para ver as mudanças aplicadas instantaneamente à caixa de teste e ao código CSS gerado ao lado.Copie: Use o botão "Copiar Código" para transferir o CSS completo (incluindo todas as propriedades manipuladas) para a sua área de transferência.💡 Estrutura do CódigoO projeto utiliza uma arquitetura limpa e baseada em eventos:index.html: Contém todos os elementos de entrada (<input type="range">, <input type="color">, <select>) e o elemento alvo (#target-box).style.css: Define o layout, utilizando position: fixed para manter a caixa de teste e o código gerado sempre visíveis.script.js:Captura todos os inputs e elementos de saída.Define uma função mestra (updateProperties()) que é chamada a cada mudança de entrada ('input' ou 'change').Dentro de updateProperties(), a lógica de cada propriedade é construída (ex: box-shadow: 10px 10px 10px #505050;) e aplicada diretamente ao style do elemento alvo, e simultaneamente, atualizada no bloco de código (#css-code).Desenvolvido por Edson Teixeira P. Junior.
